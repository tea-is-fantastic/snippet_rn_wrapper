export type Insets = {
  top: number | string;
  bottom: number | string;
  left: number | string;
  right: number | string;
};

export interface Dimensions {
  vertical: Insets;
  horizontal: string;
}
